// program for nested if  else using operators
#include <stdio.h>

int main()
{
   int year;
   printf("enter the  year");
   scanf("%d",&year);
   if(year%100!=0 && year%4==0 || year%400==0)
  
      
      printf("leap year\n");
      
      else
       printf("not leap year\n");
   }
// program for nested if  else using operators
#include <stdio.h>

int main()
{
   int year;
   printf("enter the  year");
   scanf("%d",&year);
   if(year%100!=0 && year%4==0 || year%400==0)
  
      
      printf("leap year\n");
      
      else
       printf("not leap year\n");
   }
